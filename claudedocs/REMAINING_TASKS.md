# 남은 작업 목록

**작성일**: 2025-11-09
**프로젝트**: SHAREZONE - 공유오피스 M&A 플랫폼

---

## ✅ 완료된 작업

### Supabase 데이터베이스 연동 (9개 페이지)
1. `/` - 메인 페이지 ✅
2. `/listings` - 매물 목록 ✅
3. `/listings/[slug]` - 매물 상세 ✅
4. `/articles` - 아티클 목록 ✅
5. `/admin/dashboard` - 대시보드 ✅
6. `/admin/listings` - 관리자 매물 목록 ✅
7. `/admin/articles` - 관리자 아티클 목록 ✅
8. `/admin/inquiries/purchase` - 인수 상담 목록 ✅
9. `/admin/inquiries/register` - 매각 상담 목록 ✅

### 관리자 인증 시스템
- `/admin/login` - 이메일 로그인 (Supabase Auth) ✅
- `middleware.ts` - 세션 관리 및 접근 제어 ✅
- `AdminHeader.tsx` - 로그아웃 기능 ✅

---

## 🔴 최우선 작업: 관리자 매물 등록/수정

### 1. 매물 등록 페이지 API 연동
**파일**: `/admin/listings/new/page.tsx`

**작업 내용**:
- 폼 제출 → `createListing()` API 호출
- TypeScript 타입 → Supabase 스키마 변환
- 성공 시 `/admin/listings` 목록으로 이동
- 에러 처리 및 유효성 검사

**API 함수**: `lib/api/listings.ts`의 `createListing()` (이미 존재)

---

### 2. 매물 수정 페이지 API 연동
**파일**: `/admin/listings/[id]/page.tsx`

**작업 내용**:
- URL 파라미터로 매물 ID 받기
- `getListingById()` → 기존 데이터 로드하여 폼 채우기
- 폼 제출 → `updateListing()` API 호출
- 성공 시 `/admin/listings` 목록으로 이동
- 삭제 기능 → `deleteListing()` (Soft Delete)

**API 함수**:
- `getListingById(id)` (이미 존재)
- `updateListing(id, updates)` (이미 존재)
- `deleteListing(id)` (이미 존재)

---

## 🟡 2순위: 이미지 업로드 기능

### 1. Supabase Storage 설정
**작업 내용**:
- Supabase 대시보드에서 Storage 버킷 생성 (`listings-images`, `articles-images`)
- Public 접근 정책 설정
- 이미지 URL 생성 로직

### 2. 이미지 업로드 UI 컴포넌트
**새 파일**: `components/admin/ImageUpload.tsx`

**기능**:
- 드래그 앤 드롭 이미지 업로드
- 이미지 미리보기
- 여러 이미지 업로드 (매물당 최대 5장)
- 업로드 진행률 표시
- 이미지 삭제 기능

### 3. 이미지 API 통합
**작업 내용**:
- 매물 등록/수정 시 이미지 업로드 처리
- `listing_images` 테이블에 URL 저장
- `display_order` 순서 관리

---

## 🟢 3순위: 아티클 관리 페이지

### 1. 아티클 등록 페이지 생성
**새 파일**: `/admin/articles/new/page.tsx`

**기능**:
- 제목, slug, 카테고리, 태그 입력
- 콘텐츠 에디터 (Markdown 또는 WYSIWYG)
- 썸네일 이미지 업로드
- Featured 아티클 설정
- 발행일 설정

### 2. 아티클 수정 페이지 생성
**새 파일**: `/admin/articles/[id]/page.tsx`

**기능**:
- 기존 아티클 불러오기
- 수정 및 저장
- 삭제 기능

### 3. 외부 블로그 연동 (선택)
**작업 내용**:
- 네이버 블로그 RSS 피드 가져오기
- 자동 동기화 스케줄러
- 중복 방지 로직

---

## ⚪ 4순위: 설정 페이지 저장 기능

### 설정 페이지 API 연동
**파일**: `/admin/settings/page.tsx`

**작업 내용**:
- 각 탭별 저장 버튼 활성화
  - 일반 설정 (사이트 정보, 운영 시간)
  - 프로필 설정 (관리자 정보)
  - 알림 설정 (이메일 알림)
  - 보안 설정 (비밀번호 변경)
  - 데이터베이스 연결 정보 (읽기 전용)

**새 테이블 필요 여부 검토**:
- `site_settings` 테이블 생성 고려
- 또는 .env.local 파일로 관리

---

## 📊 현재 프로젝트 통계

### 전체 페이지 수: 15개
- ✅ Supabase 연동 완료: 9개
- 🟡 UI 완성, API 연동 필요: 3개 (매물 등록/수정, 로그인)
- 🟢 리다이렉트 전용: 2개
- 🆕 설정 페이지: 1개

### 데이터베이스 테이블
- `listings` - 매물 ✅
- `listing_images` - 매물 이미지 ✅
- `articles` - 아티클 ✅
- `purchase_inquiries` - 인수 상담 ✅
- `register_inquiries` - 매각 상담 ✅
- `admins` - 관리자 (Auth 테이블 사용)

---

## 🛠 기술 스택

- **Frontend**: Next.js 16.0.1 (App Router)
- **UI**: shadcn/ui + Tailwind CSS
- **Database**: Supabase (PostgreSQL)
- **Auth**: Supabase Auth
- **Storage**: Supabase Storage (예정)
- **Language**: TypeScript

---

## 📝 주요 작업 파일 위치

### API 함수
- `lib/api/listings.ts` - 매물 CRUD
- `lib/api/articles.ts` - 아티클 CRUD
- `lib/api/inquiries.ts` - 상담 관리
- `lib/supabase.ts` - Supabase 클라이언트

### 타입 정의
- `lib/types.ts` - TypeScript 타입

### 컴포넌트
- `components/admin/` - 관리자 컴포넌트
- `components/ui/` - shadcn/ui 컴포넌트

### 페이지
- `app/(main)/` - 사용자 페이지
- `app/admin/` - 관리자 페이지

---

## 💡 다음 세션 시작 시

1. **매물 등록 페이지** `/admin/listings/new` API 연동부터 시작
2. 폼 제출 → Supabase 저장 로직 구현
3. 성공/실패 처리 및 사용자 피드백

---

**마지막 업데이트**: 2025-11-09
**다음 작업 우선순위**: 매물 등록/수정 API 연동
