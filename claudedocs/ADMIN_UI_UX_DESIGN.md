# SHAREZONE 관리자 페이지 UI/UX 설계

**작성일**: 2025-11-08
**프로젝트**: SHAREZONE (쉐어존) - 공유오피스 M&A 플랫폼
**목적**: 관리자 페이지 UI/UX 설계 및 구현 가이드

---

## 📐 디자인 시스템 (기존 프로젝트 기반)

### 색상 팔레트
```typescript
// Primary
Toss Blue: #0064FF (--primary: 217 100% 50%)

// Grey Scale
grey-50:  #f9fafb   // 배경
grey-100: #f3f4f6   // 카드 배경
grey-200: #e5e7eb   // Border
grey-700: #374151   // 텍스트
grey-900: #111827   // 제목

// Semantic Colors
success: #10b981   // 성공, 활성 상태
warning: #f59e0b   // 경고, 대기 상태
error:   #ef4444   // 에러, 거부 상태
```

### 타이포그래피
```typescript
// Font Family
Pretendard Variable

// Font Sizes
body:     18px / 1.6 / -0.01em
sub:      20px / 1.5 / -0.01em / 500
main:     24px / 1.4 / -0.02em / 700
main-lg:  32px / 1.3 / -0.02em / 700
main-xl:  40px / 1.2 / -0.03em / 700
```

### Spacing
```
Container: max-w-7xl (1280px)
Padding: px-4 sm:px-6 lg:px-8
Gap: gap-4 (1rem), gap-6 (1.5rem), gap-8 (2rem)
```

---

## 🗂️ 정보 구조 (IA)

```
SHAREZONE 관리자 페이지
│
├─ 🔐 로그인 (/admin)
│   └─ 이메일 + 비밀번호 인증
│
├─ 📊 대시보드 (/admin/dashboard)
│   ├─ 통계 카드 (매물, 상담, 조회수)
│   ├─ 최근 활동 타임라인
│   └─ 빠른 액션 버튼
│
├─ 🏢 매물 관리 (/admin/listings)
│   ├─ 매물 목록 (/admin/listings)
│   │   ├─ 검색/필터 (상태, 지역, 가격)
│   │   ├─ 정렬 (최신순, 가격순, 조회수순)
│   │   └─ 일괄 작업 (상태 변경, 삭제)
│   ├─ 신규 등록 (/admin/listings/new)
│   │   ├─ 기본 정보 (제목, 지역, 면적, 룸 개수)
│   │   ├─ 재정 정보 (가격, 권리금, 투자비용, 월수익)
│   │   ├─ 이미지 업로드 (최대 8장, 드래그앤드롭)
│   │   └─ 설명 (마크다운 에디터)
│   ├─ 수정 (/admin/listings/[id])
│   │   └─ 등록과 동일한 폼 + 미리보기
│   └─ 히스토리 (/admin/listings/[id]/history)
│       └─ 변경 이력 타임라인 (누가, 언제, 무엇을)
│
├─ 💬 상담 관리 (/admin/inquiries)
│   ├─ 인수 상담 (/admin/inquiries/purchase)
│   │   ├─ 상담 목록 (상태별 필터, 매물별 필터)
│   │   ├─ 상담 상세 (신청자 정보, 문의 내용)
│   │   ├─ 상태 변경 (대기 → 연락 → 검증 → 전환 / 거부)
│   │   ├─ 담당자 배정
│   │   └─ 관리자 메모
│   └─ 매각 상담 (/admin/inquiries/register)
│       ├─ 상담 목록
│       ├─ 상담 상세
│       ├─ 매물 연결 기능 (상담 → 매물 생성)
│       └─ 관리자 메모
│
├─ 📝 콘텐츠 관리 (/admin/articles)
│   ├─ 아티클 목록 (/admin/articles)
│   ├─ 작성 (/admin/articles/new)
│   │   ├─ 제목, 카테고리, 요약
│   │   ├─ 마크다운 에디터 (실시간 미리보기)
│   │   └─ 썸네일 업로드
│   ├─ 수정 (/admin/articles/[id])
│   └─ 블로그 연동 (/admin/articles/import)
│       └─ 티스토리/네이버 블로그 API 연동
│
└─ ⚙️ 설정 (/admin/settings)
    ├─ 이메일 알림 설정
    ├─ 관리자 계정 관리 (super_admin만)
    └─ 프로필 수정
```

---

## 🎨 레이아웃 설계

### 1. 로그인 페이지 (`/admin`)

**레이아웃**: 중앙 정렬 카드

```
┌────────────────────────────────────────┐
│                                        │
│          [SHAREZONE 로고]              │
│                                        │
│     ┌──────────────────────┐           │
│     │                      │           │
│     │  관리자 로그인       │           │
│     │                      │           │
│     │  이메일              │           │
│     │  [____________]      │           │
│     │                      │           │
│     │  비밀번호            │           │
│     │  [____________]      │           │
│     │                      │           │
│     │  [ 로그인 ]          │           │
│     │                      │           │
│     └──────────────────────┘           │
│                                        │
└────────────────────────────────────────┘
```

**컴포넌트**:
- Card (shadcn/ui)
- Input (이메일, 비밀번호)
- Button (로그인)

---

### 2. 관리자 레이아웃 (로그인 후 공통)

**레이아웃**: 고정 사이드바 + 메인 콘텐츠

```
┌─────────────────────────────────────────────────────────┐
│ [로고]  SHAREZONE Admin          [관리자명] [로그아웃] │ ← Header (고정)
├──────────┬──────────────────────────────────────────────┤
│          │                                              │
│ 📊 대시보드│  ← 메인 콘텐츠 영역                          │
│          │  (각 페이지 내용)                            │
│ 🏢 매물   │                                              │
│          │                                              │
│ 💬 상담   │                                              │
│          │                                              │
│ 📝 콘텐츠 │                                              │
│          │                                              │
│ ⚙️ 설정   │                                              │
│          │                                              │
│ ← 사이드바│                                              │
│  (고정)  │                                              │
│          │                                              │
└──────────┴──────────────────────────────────────────────┘
```

**구조**:
```typescript
// app/admin/layout.tsx
<div className="flex h-screen bg-grey-50">
  {/* Sidebar */}
  <aside className="w-64 bg-white border-r border-grey-200">
    <Sidebar />
  </aside>

  {/* Main Content */}
  <div className="flex-1 flex flex-col overflow-hidden">
    {/* Top Header */}
    <header className="bg-white border-b border-grey-200 px-6 py-4">
      <AdminHeader />
    </header>

    {/* Scrollable Content */}
    <main className="flex-1 overflow-y-auto p-6">
      {children}
    </main>
  </div>
</div>
```

---

## 📄 페이지별 상세 기획

### 📊 대시보드 (`/admin/dashboard`)

**목적**: 핵심 지표 모니터링 및 빠른 액션

**레이아웃**:
```
┌─────────────────────────────────────────────────┐
│ 대시보드                                         │
├─────────────────────────────────────────────────┤
│                                                 │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐         │
│ │ 📊 총 매물│ │ 💬 대기중│ │ 👁️ 총 조회│         │
│ │   24개   │ │ 상담 12건│ │  1,234회 │         │
│ │ +3 이번주│ │ +5 신규  │ │ +234 주간│         │
│ └──────────┘ └──────────┘ └──────────┘         │
│                                                 │
│ ┌─────────────────┐ ┌───────────────┐          │
│ │ 📈 최근 활동     │ │ 🚀 빠른 액션   │          │
│ │                 │ │               │          │
│ │ • 강남 오피스   │ │ [+ 매물 등록] │          │
│ │   등록됨 (2시간)│ │               │          │
│ │                 │ │ [💬 상담 확인]│          │
│ │ • 인수 상담     │ │               │          │
│ │   5건 (오늘)    │ │ [📝 글 작성] │          │
│ │                 │ │               │          │
│ └─────────────────┘ └───────────────┘          │
│                                                 │
└─────────────────────────────────────────────────┘
```

**컴포넌트**:
- Card (통계 카드 3개)
- Badge (변화량 표시)
- Button (빠른 액션)
- Timeline 커스텀 컴포넌트

**데이터 소스**:
```typescript
// API: /api/admin/dashboard
{
  totalListings: number
  totalInquiries: number
  totalViews: number
  recentActivities: Activity[]
}
```

---

### 🏢 매물 목록 (`/admin/listings`)

**목적**: 전체 매물 관리 및 상태 변경

**레이아웃**:
```
┌──────────────────────────────────────────────────┐
│ 매물 관리                          [+ 신규 등록]  │
├──────────────────────────────────────────────────┤
│                                                  │
│ [검색: ________] [상태: ▼] [지역: ▼] [정렬: ▼]  │
│                                                  │
│ ┌────────────────────────────────────────────┐  │
│ │ ☑ SZ-001 | 강남 프리미엄 오피스 | 서울    │  │
│ │    3.2억 | 165㎡ | 10개 룸 | [판매중] ▼   │  │
│ │    조회 234회 | 2025-11-05               │  │
│ │    [수정] [히스토리] [삭제]               │  │
│ ├────────────────────────────────────────────┤  │
│ │ ☑ SZ-002 | 서초 스타트업 공간 | 서울     │  │
│ │    1.8억 | 99㎡ | 6개 룸 | [검토중] ▼    │  │
│ │    조회 89회 | 2025-11-03                │  │
│ │    [수정] [히스토리] [삭제]               │  │
│ └────────────────────────────────────────────┘  │
│                                                  │
│ [이전] 1 2 3 [다음]                              │
│                                                  │
└──────────────────────────────────────────────────┘
```

**기능**:
- 검색: 제목, 매물번호, 설명 검색
- 필터: 상태 (판매중/검토중/거래완료/숨김), 지역, 가격 범위
- 정렬: 최신순, 가격순, 조회수순
- 일괄 작업: 체크박스로 여러 매물 선택 → 상태 변경/삭제
- 상태 변경: 드롭다운으로 즉시 변경

**컴포넌트**:
- Input (검색)
- Select (필터, 정렬)
- Table (매물 목록)
- Checkbox (일괄 선택)
- DropdownMenu (액션)
- Badge (상태 표시)
- Pagination

---

### 🏢 매물 등록/수정 (`/admin/listings/new`, `/admin/listings/[id]`)

**목적**: 매물 정보 입력 및 수정

**레이아웃**: 탭 형식 (기본 정보 → 재정 정보 → 이미지 → 설명)

```
┌────────────────────────────────────────────────┐
│ 매물 등록                       [미리보기] [저장]│
├────────────────────────────────────────────────┤
│                                                │
│ [기본정보] [재정정보] [이미지] [설명]          │
│ ─────────                                      │
│                                                │
│ 매물 제목 *                                    │
│ [_______________________________]              │
│                                                │
│ 지역 *        면적(㎡) *    룸 개수 *         │
│ [서울 ▼]     [_____]       [____]             │
│                                                │
│ 개업일        운영 상태                        │
│ [2023-05-01]  [운영중 ▼]                      │
│                                                │
│ 상태 *        프리미엄                         │
│ [판매중 ▼]    [☑ 프리미엄 매물로 표시]          │
│                                                │
│                             [다음: 재정 정보 →]│
└────────────────────────────────────────────────┘
```

**섹션 1: 기본 정보**
- 매물 제목 (Input)
- 지역 (Select: 서울, 경기 등)
- 면적 ㎡ (Input number)
- 룸 개수 (Input number)
- 개업일 (Calendar)
- 운영 상태 (Select: 운영중, 휴업)
- 상태 (Select: 판매중, 검토중, 숨김, 거래완료)
- 프리미엄 (Checkbox)

**섹션 2: 재정 정보**
- 매매가 (Input number) + 협의가능 (Checkbox)
- 권리금 (Input number)
- 총 투자비용 (Input number)
- 월수익 (Input number)

**섹션 3: 이미지** (최대 8장)
```
┌──────────────────────────────────────┐
│ 이미지 업로드 (최대 8장)             │
│                                      │
│ ┌────┐ ┌────┐ ┌────┐ ┌────┐         │
│ │ 1  │ │ 2  │ │ 3  │ │ + │         │
│ │[대표]│ │    │ │    │ │   │         │
│ └────┘ └────┘ └────┘ └────┘         │
│                                      │
│ • 드래그앤드롭으로 이미지 업로드     │
│ • 첫 번째 이미지가 대표 이미지       │
│ • 순서 변경: 드래그로 재배치         │
│                                      │
└──────────────────────────────────────┘
```

**섹션 4: 설명**
- 짧은 설명 (Textarea, 100자)
- 상세 설명 (Textarea, 1000자)

**컴포넌트**:
- Tabs (섹션 구분)
- Input, Textarea
- Select, Calendar
- Checkbox, Switch
- 이미지 업로드 (react-dropzone)
- Button (저장, 미리보기)

---

### 💬 상담 관리 (`/admin/inquiries/purchase`)

**목적**: 인수/매각 상담 응대 및 상태 관리

**레이아웃**:
```
┌──────────────────────────────────────────────┐
│ 인수 상담 관리                                │
├──────────────────────────────────────────────┤
│                                              │
│ [전체: 24] [대기: 12] [연락완료: 8] [전환: 4]│
│                                              │
│ ┌──────────────────────────────────────────┐│
│ │ 🟡 대기 | 김철수 | 010-1234-5678        ││
│ │   목적: 투자 | 예산: 3억 내외            ││
│ │   매물: SZ-001 강남 프리미엄 오피스      ││
│ │   신청일: 2025-11-08 14:30              ││
│ │   [상세보기] [연락완료 처리]             ││
│ ├──────────────────────────────────────────┤│
│ │ 🟢 연락완료 | 이영희 | 010-5678-9012    ││
│ │   담당자: 나성호 | 메모: 방문 예약 완료 ││
│ │   매물: SZ-003 | 신청일: 2025-11-07    ││
│ │   [상세보기] [검증 완료 처리]            ││
│ └──────────────────────────────────────────┘│
│                                              │
└──────────────────────────────────────────────┘
```

**상담 상세 모달**:
```
┌────────────────────────────────────┐
│ 상담 상세                  [X]     │
├────────────────────────────────────┤
│                                    │
│ 📋 신청자 정보                     │
│ 이름: 김철수                       │
│ 전화: 010-1234-5678                │
│ 이메일: kim@example.com            │
│                                    │
│ 📝 문의 내용                       │
│ 목적: 투자                         │
│ 예산: 3억 내외                     │
│ 경험: 있음                         │
│ 메시지: 강남 지역 관심 있습니다... │
│                                    │
│ 🏢 연결 매물                       │
│ SZ-001 | 강남 프리미엄 오피스      │
│ [매물 보기]                        │
│                                    │
│ 👤 담당자 배정                     │
│ [나성호 ▼]                         │
│                                    │
│ 📝 관리자 메모                     │
│ [_______________________]          │
│                                    │
│ 🔄 상태 변경                       │
│ [대기중 ▼]                         │
│                                    │
│ [저장] [닫기]                      │
│                                    │
└────────────────────────────────────┘
```

**상태 흐름**:
```
대기(pending) → 연락완료(contacted) → 검증완료(qualified)
                                    → 전환(converted)
                                    → 거부(rejected)
```

**컴포넌트**:
- Tabs (상태별 탭)
- Badge (상태 표시)
- Dialog (상세 모달)
- Select (담당자 배정, 상태 변경)
- Textarea (관리자 메모)

---

## 🎨 필요한 shadcn/ui 컴포넌트

### 이미 설치된 컴포넌트
- ✅ Button
- ✅ Input
- ✅ Textarea
- ✅ Dialog
- ✅ Card
- ✅ Badge

### 추가로 설치할 컴포넌트
```bash
# 관리자 페이지 필수 컴포넌트
npx shadcn@latest add table
npx shadcn@latest add select
npx shadcn@latest add dropdown-menu
npx shadcn@latest add tabs
npx shadcn@latest add checkbox
npx shadcn@latest add switch
npx shadcn@latest add calendar
npx shadcn@latest add avatar
npx shadcn@latest add separator
npx shadcn@latest add toast
npx shadcn@latest add alert-dialog
npx shadcn@latest add form
npx shadcn@latest add label
npx shadcn@latest add scroll-area
```

---

## 🚀 커스텀 컴포넌트

### 1. Sidebar (관리자 네비게이션)
**위치**: `components/admin/Sidebar.tsx`

```typescript
const menuItems = [
  { icon: LayoutDashboard, label: '대시보드', href: '/admin/dashboard' },
  { icon: Building2, label: '매물 관리', href: '/admin/listings' },
  { icon: MessageSquare, label: '상담 관리', href: '/admin/inquiries' },
  { icon: FileText, label: '콘텐츠', href: '/admin/articles' },
  { icon: Settings, label: '설정', href: '/admin/settings' },
]
```

### 2. StatCard (통계 카드)
**위치**: `components/admin/StatCard.tsx`

```typescript
interface StatCardProps {
  title: string
  value: string | number
  change?: string
  icon: LucideIcon
  trend?: 'up' | 'down' | 'neutral'
}
```

### 3. ListingStatusBadge (매물 상태 뱃지)
**위치**: `components/admin/ListingStatusBadge.tsx`

```typescript
const statusConfig = {
  active: { label: '판매중', color: 'success' },
  pending: { label: '검토중', color: 'warning' },
  hidden: { label: '숨김', color: 'grey' },
  sold: { label: '거래완료', color: 'grey' },
}
```

### 4. InquiryStatusBadge (상담 상태 뱃지)
**위치**: `components/admin/InquiryStatusBadge.tsx`

```typescript
const statusConfig = {
  pending: { label: '대기', color: 'warning' },
  contacted: { label: '연락완료', color: 'success' },
  qualified: { label: '검증완료', color: 'primary' },
  converted: { label: '전환', color: 'success' },
  rejected: { label: '거부', color: 'error' },
}
```

---

## 📱 반응형 설계

### 데스크톱 (1024px+)
- 고정 사이드바 (240px 너비)
- 2단 레이아웃 (사이드바 + 메인)
- 테이블 전체 표시

### 태블릿 (768px ~ 1023px)
- 축소형 사이드바 (아이콘만)
- 테이블 스크롤

### 모바일 (0 ~ 767px)
- 햄버거 메뉴 → 드로어 사이드바
- 카드 형식으로 전환
- 테이블 → 카드 리스트

---

## 🎯 주요 인터랙션

### 1. 매물 상태 즉시 변경
```typescript
// 드롭다운에서 선택 → API 호출 → UI 즉시 업데이트
onChange={(newStatus) => {
  updateListingStatus(id, newStatus)
  toast.success('상태가 변경되었습니다')
}}
```

### 2. 이미지 드래그앤드롭
```typescript
// react-dropzone 사용
<Dropzone
  onDrop={handleImageUpload}
  maxFiles={8}
  accept={{ 'image/*': ['.png', '.jpg', '.jpeg', '.webp'] }}
/>
```

### 3. 실시간 검색
```typescript
// debounce 적용 (300ms)
const debouncedSearch = useDebouncedCallback((query) => {
  fetchListings({ search: query })
}, 300)
```

---

## 🔐 권한 관리

### 역할별 권한
```typescript
// super_admin
- 모든 기능 접근
- 관리자 계정 생성/삭제
- 시스템 설정

// admin
- 매물 CRUD
- 상담 관리
- 콘텐츠 관리

// staff
- 매물 조회/수정 (삭제 불가)
- 상담 조회/응대
```

### 권한 체크
```typescript
// 컴포넌트에서 권한 확인
if (user.role === 'super_admin') {
  return <DeleteButton />
}
```

---

## 📊 데이터 흐름

```
사용자 액션
  ↓
React 컴포넌트 (상태 관리)
  ↓
API 라우트 호출 (/api/admin/*)
  ↓
Supabase 클라이언트 (sharezone 스키마)
  ↓
PostgreSQL (cursor-mcp-dev)
  ↓
응답 데이터
  ↓
UI 업데이트 + Toast 알림
```

---

## 🎨 디자인 참고

### 관리자 페이지 벤치마크
- **Notion**: 깔끔한 사이드바, 계층 구조
- **Linear**: 심플한 테이블, 빠른 상태 변경
- **Vercel Dashboard**: 통계 카드, 미니멀 디자인

### 색상 사용 가이드
```typescript
// 상태별 색상
판매중 (active):    success (#10b981)
검토중 (pending):   warning (#f59e0b)
거래완료 (sold):    grey-400 (#9ca3af)
숨김 (hidden):      grey-400

// 상담 상태 색상
대기 (pending):     warning
연락완료 (contacted): success
검증완료 (qualified): primary (Toss Blue)
전환 (converted):   success
거부 (rejected):    error
```

---

## 📝 다음 단계

### Phase 2: 구현 순서
1. ✅ **관리자 레이아웃** (`app/admin/layout.tsx`)
   - Sidebar 컴포넌트
   - AdminHeader 컴포넌트

2. ✅ **대시보드** (`app/admin/dashboard/page.tsx`)
   - StatCard 컴포넌트
   - 통계 API 연동

3. ✅ **매물 목록** (`app/admin/listings/page.tsx`)
   - Table 컴포넌트
   - 검색/필터/정렬

4. ✅ **매물 등록/수정** (`app/admin/listings/new/page.tsx`)
   - Form 컴포넌트
   - 이미지 업로드

5. ✅ **상담 관리** (`app/admin/inquiries/purchase/page.tsx`)
   - 상담 목록
   - 상세 모달

6. ✅ **콘텐츠 관리** (`app/admin/articles/page.tsx`)
   - 마크다운 에디터

---

**문서 작성 완료!** 🎉

이제 이 설계를 바탕으로 컴포넌트를 구현할 준비가 되었습니다.
